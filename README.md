# 旅行社组团与计调操作平台

面向旅行社**销售部**与**计调部**的一体化作业系统：销售建产品/收客，计调锁机位/控房/确认地接，实时核算单团毛利，一键生成并发送出团通知书。

技术栈：**React 18 + Vite**（前端）· **Express + better-sqlite3（SQLite）**（后端），无外部数据库依赖，数据文件位于 `server/../data/travel.db`。

## 快速开始

```bash
./start.sh              # 一键启动（生产模式）：自动装依赖/编译原生模块、首次灌种子、构建前端、单端口启动
./start.sh dev          # 开发模式：API(4000) + Vite(5173) 热更新并行
./start.sh seed         # 仅重置演示数据
npm run install:all     # 仅安装后端与前端依赖
npm run seed            # 写入演示数据（3条线路/3个团、38名游客、5家供应商、7条采购资源、8条占用，含预警场景）
npm run dev             # 同时启动 API(4000) 与前端开发服务器(5173)
npm test                # 运行库存冲突/并发/跨日期控房/释放/成本快照/采购预警测试（独立临时库）
```

> 如本机 better-sqlite3 需要本地编译，可指定系统编译器：`CC=/usr/bin/gcc CXX=/usr/bin/g++ npm install`

打开 http://localhost:5173 （开发模式自动代理 `/api`）。

生产模式（单端口托管）：

```bash
npm run build && npm start   # http://localhost:4000 直接访问
```

## 功能模块

### 1. 旅游产品（销售部）
- 线路名称、行程天数、出发城市、目的地
- 三档报价：双人房 / 三人房 / 儿童不占床
- 按天维护行程：当日主题、景点、用餐、住宿；调整天数自动增删日程行
- 产品可多次开团；已被团队引用的产品禁止删除（保留历史档案）

### 2. 团队与收客
- 选产品开团：出发/返程日期、团容量、领队；团号按出发日期自动生成（`TH20261001-001`），也可手工指定
- 录入游客：姓名、身份证号（**18 位 + GB 校验位验证**）、手机号、房型、特殊需求（素食/轮椅/过敏等）、实收团费（默认带产品标准价）
- 同团身份证去重；**达到容量自动停止收客**（按钮禁用 + 红色横幅），状态非「收客中」同样禁止录客
- 退团自动释放名额；团队状态流转：收客中 → 已截止 → 已成团 → 已出团 → 已结束（可回退）
- 收客进度条、房型人数与报名费收入实时统计

### 3. 计调操作（计调部）
- **资源池占位（推荐）**：从供应商资源池选择航班座位/酒店房量/地接容量发起占用，联动生成计调记录；状态流转 **待确认 → 已确认 → 已释放**
- **航空切位**：去/回程航班号、日期、航线、切位座位数、切位价，待确认/已确认（支持资源池占位与手工录入两种方式）
- **酒店控房**：酒店、房型、间数、入住/离店日期、晚均价；自动按晚数计成本
- **地接社确认**：地接社、地接导游及电话、用车、用餐安排、打包费用、确认状态
- **其他成本**：门票、保险、领队费用等杂项
- **手工兼容**：未关联资源池的历史/手工计调记录保持原有全部行为，不受库存校验约束（表中以 📦 标识资源池联动行）

### 3.5 供应商资源池与库存冲突控制
- **供应商**：航司/包机商、酒店、地接社三类，统一维护联系人、合作状态
- **采购资源（库存）**：供应商、日期、采购数量、采购单价、状态（在售/停售）；酒店为「起始入住日~离店日 + **每日房量**」
- **实时余量**：余量 = 采购量 − 所有有效占用（待确认 + 已确认，已释放不计）；酒店**按入住日期逐日校验**（离店日不占房），跨日期占用仅占用区间内的日期
- **超卖防护**：创建/修改/确认占用均在数据库事务（`BEGIN IMMEDIATE` + busy_timeout 串行化）内完成，先校验余量再写入；并发请求不会超卖
- **冲突响应**：HTTP 409 返回每个冲突日期的采购量/已占用/剩余量/缺口，例如
  `「三亚亚特兰蒂斯酒店」库存冲突：2026-10-03（余量 10/20，缺 5）`
- **成本快照**：确认占用时按当时采购单价锁定 `price_snapshot` 与 `cost_snapshot`；之后供应商调价**不影响已确认团队毛利**。待确认占用的计调行单价跟随采购价浮动；确认后即冻结
- **释放规则**：① 计调手工释放 → 删除联动计调行、恢复余量；② 游客全部退团 → 自动释放该团**待确认**占用（已确认涉及供应商成约，保留由计调处理）；③ 删除采购资源 → 事务内释放全部有效占用；④ 删除/取消团队 → 释放全部占用
- 团队计调页展示实时余量（含逐日视图）、占用来源团队、超卖红色冲突提示；已释放记录保留审计留痕

### 3.6 采购资源预警中心（计调风险雷达，只读）
- 独立只读分析模块（`server/warnings.js`），**不改动任何库存逻辑**：余量复用 `inventory.getAvailability`，成本/计调数据复用 `calcFinance` 同源的计调表
- 五类预警（默认扫描未来 60 天，窗口可切换 7/30/60/90 天）：
  1. **低余量** 📉：窗口内资源（酒店按逐日）最低余量 ≤20% 为中风险，余量为 0 为高风险；仅对存在有效占用的资源预警，给出紧张日期清单
  2. **待确认超时** ⏳：占用创建后超过 24 小时未确认（中风险），超过 72 小时升级高风险
  3. **停售仍占用** 🚫：资源已停售但仍挂有待确认/已确认有效占用，统一高风险
  4. **成本价偏离** 💹：已确认占用的成本快照单价与当前采购价偏离 ≥10%（中）/≥30%（高），区分涨价（重新采购更贵）与降价（团队锁贵了），酒店按间夜×晚数估算影响金额
  5. **临行未确认** 🧳：出发前 7 天内的团队，航班（去/回程齐全且已确认）、酒店、地接任一缺失或有待确认行，按距出发天数 0-2/3-4/5-7 天评定高/中/低风险
- 预警页支持按**风险等级、预警类型、资源类型、供应商、出发/服务日期区间**筛选；等级卡片与类型胶囊同时是筛选器并显示实时计数
- 每条预警可一键跳转**资源详情**（自动打开余量/占用来源弹窗）或**团队详情**（计调标签页）
- 工作台顶部展示预警横幅入口；阈值（窗口/超时小时数/偏离比例等）均可通过 API 参数覆盖

### 4. 收入 vs 成本毛利估算（单团实时）
- 收入：在团游客实收团费合计
- 支出：切位（座位数×切位价）+ 控房（间数×晚均价×晚数）+ 地接 + 其他
- 预估毛利与毛利率，成本超收入时红色预警；工作台汇总全部团队

### 5. 出团通知书
- 一键**自动生成**：行程信息、航班、每日行程（景点/用餐/住宿）、酒店、地接导游/用车/用餐、游客特殊需求清单
- 生成后可在线编辑；存档后可「发送给游客」（当前为模拟发送并记录发送时间/人数，实际对接短信/邮件网关时替换 `POST /api/notices/:id/send` 即可）
- 支持预览、打印 / 另存 PDF

## 目录结构

```
server/
  index.js          Express 入口（API + 生产静态托管）
  db.js             SQLite 表结构（products/itinerary_days/tours/tourists/
                    flight_bookings/hotel_bookings/local_services/other_costs/notices/
                    suppliers/resources/resource_allocations）
  inventory.js      资源池核心：事务占用/确认/释放、逐日余量、冲突检测、成本快照
  warnings.js       采购预警中心（只读）：低余量/待确认超时/停售占用/成本偏离/临行未确认
  routes.js         全部业务接口与校验、满团规则、资源池 API、预警 API、通知书文案生成
  helpers.js        身份证校验、财务计算、团号生成
  inventory.test.js 并发占用/跨日期控房/释放规则/成本快照测试（node --test）
  warnings.test.js  五类预警规则/阈值/筛选/只读性测试（node --test）
  seed.js           演示数据
client/src/
  pages/            工作台、产品列表/编辑、团队列表/新建/详情、供应商资源池、采购预警中心
  components/tour/  团队详情标签页：收客 / 计调(含资源池占用) / 财务 / 通知书
start.sh            一键启动脚本（prod / dev / seed）
```

## 主要 API

| 方法 | 路径 | 说明 |
|---|---|---|
| GET/POST | `/api/products` `/api/products/:id` | 产品 CRUD（含 itinerary 日程数组） |
| GET/POST | `/api/tours` `/api/tours/:id` | 团队列表/详情（聚合游客、计调、占用、财务） |
| POST | `/api/tours/:id/tourists` | 报名（满团/校验/去重拦截） |
| PATCH/DELETE | `/api/tourists/:id` | 退团（整团退完自动释放待确认占用）、修改、删除 |
| POST | `/api/tours/:id/flights` `/hotels` `/local-services` `/other-costs` | 手工计调资源（兼容旧流程） |
| GET/POST/PUT/DELETE | `/api/suppliers` `/api/suppliers/:id` | 供应商 |
| GET/POST/PUT/DELETE | `/api/resources` `/api/resources/:id` | 采购资源（改量/改期会校验既有占用） |
| GET | `/api/resources/:id/availability` | 逐日余量 + 占用来源 |
| GET/POST | `/api/allocations` | 占用列表（可按 tour/resource/status 过滤）/创建占用（事务，409 返冲突日期与余量） |
| PUT | `/api/allocations/:id` | 修改待确认占用（数量/日期，重新做冲突校验） |
| POST | `/api/allocations/:id/confirm` | 确认占用（事务 + 锁定成本快照） |
| POST | `/api/allocations/:id/release` | 释放占用（删联动计调行、恢复余量） |
| POST | `/api/tours/:id/notices/generate` | 自动生成通知书文案 |
| POST | `/api/notices/:id/send` | 发送（模拟网关） |
| GET | `/api/warnings` | 采购预警列表（筛选：`level`/`types`/`resource_type`/`supplier_id`/`date_from`/`date_to`；阈值：`horizonDays`/`staleHours`/`driftRatio` 等） |
| GET | `/api/warnings/summary` | 预警概要计数（高/中/低 + 分类计数） |
| GET | `/api/stats` | 工作台汇总（含 `warnings` 预警概要） |
